# Wave B runtime datasets

`runtime.json` is generated by `perf/scripts/wave-b-seed.mjs` inside the isolated benchmark environment and is intentionally not committed as static user data.

The generated file contains only synthetic Wave B guest identities/tokens and run-scoped game IDs. It must never contain production credentials or production user data.
