# P1-C-01 — Checkers session CAS

Patch scope only. No merge or deployment authorization.

Implemented:
- additive `version INTEGER NOT NULL DEFAULT 1` on `gracz_game_sessions`;
- `getVersioned(gameId)`;
- compare-and-swap `UPDATE ... WHERE version = expectedVersion`;
- `SESSION_CONCURRENCY_CONFLICT` with HTTP 409 semantics;
- version increment on successful write;
- stale-write preservation and explicit retry tests;
- all five HTTP mutation paths verified to avoid realtime publish after conflict;
- real PostgreSQL coverage in CI.
