# P1-AUD3-04 — Gomoku durable persistence / concurrency / recovery

Scope:
- PostgreSQL-backed durable Gomoku game state.
- Revision-based atomic CAS writes.
- Stable `GOMOKU_CONCURRENCY_CONFLICT` / HTTP 409 contract.
- Idempotent request retry by `requestId`.
- Recovery after service restart.
- Existing in-memory `GomokuService` remains available for dev/tests.

Acceptance gate:
- real PostgreSQL 16 tests for restart recovery, concurrent stale writes and idempotent retry,
- generic CheckersEngine/Security Gate remain green,
- no production deployment or migration execution in this patch.
