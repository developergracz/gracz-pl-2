import { createCipheriv, createDecipheriv, hkdfSync, randomBytes, randomUUID } from "node:crypto";
import pg from "pg";
import { AccountError } from "./accounts.js";
const { Pool }=pg;
const MAX_ATTACHMENT_BYTES=1024*1024;
const ALLOWED_TYPES=new Set(["image/png","image/jpeg"]);
const EXTENSIONS=Object.freeze({"image/png":".png","image/jpeg":".jpg"});

export class MessageAttachmentService{
  constructor(connectionString,encryptionSecret){if(typeof connectionString!=="string"||!connectionString.trim())throw new TypeError("DATABASE_URL jest wymagany.");if(typeof encryptionSecret!=="string"||encryptionSecret.length<32)throw new TypeError("Sekret szyfrowania załączników musi mieć co najmniej 32 znaki.");this.key=Buffer.from(hkdfSync("sha256",Buffer.from(encryptionSecret,"utf8"),Buffer.from("gracz.pl/message-attachments/v1","utf8"),Buffer.from("private-message-attachment-encryption","utf8"),32));this.pool=new Pool({connectionString,ssl:connectionString.includes("localhost")||connectionString.includes("127.0.0.1")?false:{rejectUnauthorized:false},max:3});this.ready=this.#initialize();}
  async #initialize(){await this.pool.query(`CREATE TABLE IF NOT EXISTS gracz_message_attachments(message_id UUID PRIMARY KEY REFERENCES gracz_messages(message_id) ON DELETE CASCADE,file_name VARCHAR(120) NOT NULL,storage_name VARCHAR(80),mime_type VARCHAR(32) NOT NULL,file_size INTEGER NOT NULL,iv BYTEA NOT NULL,auth_tag BYTEA NOT NULL,ciphertext BYTEA NOT NULL,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`);await this.pool.query(`ALTER TABLE gracz_message_attachments ADD COLUMN IF NOT EXISTS storage_name VARCHAR(80)`);}
  async save(userId,messageId,input={}){await this.ready;assertMessageId(messageId);const{rows}=await this.pool.query(`SELECT m.sender_id,m.read_at,m.sender_deleted,EXISTS(SELECT 1 FROM gracz_message_attachments a WHERE a.message_id=m.message_id) AS has_attachment FROM gracz_messages m WHERE m.message_id=$1`,[messageId]);const message=rows[0];if(!message)throw new AccountError("Nie znaleziono wiadomości.","MESSAGE_NOT_FOUND");if(message.sender_id!==userId||message.sender_deleted)throw new AccountError("Załącznik może dodać tylko nadawca wiadomości.","INVALID_MESSAGE");if(message.has_attachment)throw new AccountError("Załącznika do wysłanej wiadomości nie można podmienić.","INVALID_ATTACHMENT");if(message.read_at)throw new AccountError("Nie można dodać załącznika po przeczytaniu wiadomości przez odbiorcę.","INVALID_ATTACHMENT");
    const mimeType=String(input.mimeType??"").toLowerCase();if(!ALLOWED_TYPES.has(mimeType))throw new AccountError("Dozwolone są tylko zrzuty ekranu PNG lub JPG/JPEG.","INVALID_ATTACHMENT");const fileName=sanitizeName(input.fileName,mimeType);assertExtension(fileName,mimeType);const base64=String(input.data??"").replace(/\s/g,"");if(!base64||base64.length>Math.ceil(MAX_ATTACHMENT_BYTES*4/3)+16||!/^[A-Za-z0-9+/]*={0,2}$/.test(base64))throw new AccountError("Załącznik ma nieprawidłowy format.","INVALID_ATTACHMENT");let data;try{data=Buffer.from(base64,"base64");}catch{data=Buffer.alloc(0);}if(!data.length)throw new AccountError("Załącznik jest pusty.","INVALID_ATTACHMENT");if(data.length>MAX_ATTACHMENT_BYTES)throw new AccountError("Zrzut ekranu może mieć maksymalnie 1 MB.","INVALID_ATTACHMENT");if(!matchesSignature(data,mimeType))throw new AccountError("Plik nie jest prawidłowym obrazem PNG lub JPG/JPEG.","INVALID_ATTACHMENT");
    const storageName=`${randomUUID()}${EXTENSIONS[mimeType]}`,iv=randomBytes(12),cipher=createCipheriv("aes-256-gcm",this.key,iv);cipher.setAAD(Buffer.from(`${messageId}:${storageName}:${mimeType}:${data.length}`,"utf8"));const ciphertext=Buffer.concat([cipher.update(data),cipher.final()]),authTag=cipher.getAuthTag();try{await this.pool.query(`INSERT INTO gracz_message_attachments(message_id,file_name,storage_name,mime_type,file_size,iv,auth_tag,ciphertext) VALUES($1,$2,$3,$4,$5,$6,$7,$8)`,[messageId,fileName,storageName,mimeType,data.length,iv,authTag,ciphertext]);}catch(error){if(error?.code==="23505")throw new AccountError("Załącznika do wysłanej wiadomości nie można podmienić.","INVALID_ATTACHMENT");throw error;}return{messageId,fileName,mimeType,fileSize:data.length};}
  async getMetaForMessages(messageIds=[]){await this.ready;if(!messageIds.length)return new Map();const{rows}=await this.pool.query("SELECT message_id,file_name,mime_type,file_size FROM gracz_message_attachments WHERE message_id = ANY($1::uuid[])",[messageIds]);return new Map(rows.map(row=>[row.message_id,{fileName:row.file_name,mimeType:row.mime_type,fileSize:row.file_size}]));}
  async get(userId,messageId){await this.ready;assertMessageId(messageId);const{rows}=await this.pool.query(`SELECT a.file_name,a.storage_name,a.mime_type,a.file_size,a.iv,a.auth_tag,a.ciphertext,m.sender_id,m.recipient_id,m.sender_deleted,m.recipient_deleted FROM gracz_message_attachments a JOIN gracz_messages m ON m.message_id=a.message_id WHERE a.message_id=$1`,[messageId]);const row=rows[0],senderCanRead=row&&row.sender_id===userId&&!row.sender_deleted,recipientCanRead=row&&row.recipient_id===userId&&!row.recipient_deleted;if(!row||(!senderCanRead&&!recipientCanRead))throw new AccountError("Nie znaleziono załącznika.","MESSAGE_NOT_FOUND");try{const decipher=createDecipheriv("aes-256-gcm",this.key,row.iv);const aad=row.storage_name?`${messageId}:${row.storage_name}:${row.mime_type}:${row.file_size}`:`${messageId}:${row.mime_type}:${row.file_size}`;decipher.setAAD(Buffer.from(aad,"utf8"));decipher.setAuthTag(row.auth_tag);const clear=Buffer.concat([decipher.update(row.ciphertext),decipher.final()]);return{fileName:row.file_name,mimeType:row.mime_type,fileSize:row.file_size,data:clear.toString("base64")};}catch{throw new AccountError("Nie można odszyfrować załącznika.","INVALID_ATTACHMENT");}}
  async close(){await this.pool.end();}
}
function assertMessageId(value){if(!/^[0-9a-f-]{36}$/i.test(String(value)))throw new AccountError("Nieprawidłowy identyfikator wiadomości.","INVALID_MESSAGE");}
function sanitizeName(value,mimeType){const fallback=mimeType==="image/png"?"zrzut-partii.png":"zrzut-partii.jpg";const cleaned=String(value||fallback).replace(/[\\/:*?"<>|\x00-\x1f]/g,"_").trim().slice(0,120);return cleaned||fallback;}
function assertExtension(fileName,mimeType){const lower=fileName.toLowerCase(),ok=mimeType==="image/png"?lower.endsWith(".png"):lower.endsWith(".jpg")||lower.endsWith(".jpeg");if(!ok)throw new AccountError("Rozszerzenie pliku nie pasuje do typu obrazu.","INVALID_ATTACHMENT");}
function matchesSignature(data,mimeType){if(mimeType==="image/png")return data.length>=16&&data.subarray(0,8).equals(Buffer.from([0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a]));return data.length>=4&&data[0]===0xff&&data[1]===0xd8&&data[2]===0xff&&data[data.length-2]===0xff&&data[data.length-1]===0xd9;}
